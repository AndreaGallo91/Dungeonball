import { GRAVITY, BALL_CONFIG, FLIPPER_CONFIG, BUMPER_CONFIG } from '../utils/constants.js';

export const PhysicsConfig = {
  gravity: GRAVITY,
  ball: BALL_CONFIG,
  flipper: FLIPPER_CONFIG,
  bumper: BUMPER_CONFIG,
};
